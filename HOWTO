

In order to run the parser, just call:
./Dotparser.py [dot file]

So for example:
./Dotparser.py temp.dot


What this will do right now is display the representation of the graph. 
We can use this to feed the graph data into our solver.